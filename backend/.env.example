# Copy to .env and fill in. Without a key the app still works:
# insights fall back to rule-based templates and the chat tab is disabled.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL=openai/gpt-oss-20b

MAX_ROWS=100000
